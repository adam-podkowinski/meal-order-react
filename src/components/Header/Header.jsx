import classes from "./Header.module.scss";

const Header = () => {
  return (
    <>
      <nav className={classes.header}>
        <h1>Meal order</h1>
        <p>siema</p>
      </nav>
    </>
  );
};

export default Header;
